import { FormNewProductComponent } from '../components'
export const Admin = () => {

 //Página exclusiva para administrador para controlar la subida de productos, se cargan las imagenes por al storage de firebase y el resto de la información directamente a la colección de products

  return (
    <FormNewProductComponent />
  )}